# Supabase — client-side (safe to expose; RLS-respecting)
NEXT_PUBLIC_SUPABASE_URL=https://rigyjvqarqxrutucvgfb.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_gjMK6ntslZ4-ccbEqCn90A__swZYumC

# Supabase — SERVER ONLY. Bypasses RLS. Used by scripts/seed.ts.
# Get from Supabase dashboard → Settings → API Keys → Secret keys.
# NEVER expose this client-side or commit it.
SUPABASE_SECRET_KEY=sb_secret_your_key_here

# Base URL of the deployed site (used for auth redirects + OG tags)
NEXT_PUBLIC_SITE_URL=http://localhost:3000
